import { ImageResizeDirective } from './image-resize.directive';

describe('ImageResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageResizeDirective();
    expect(directive).toBeTruthy();
  });
});
